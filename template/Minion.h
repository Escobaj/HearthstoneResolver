#ifndef HEARTHSTONERESOLVER_{$id|upper}_H
#define HEARTHSTONERESOLVER_{$id|upper}_H


#include "../Core/Entities/Minion.h"

class {$id} : public Minion {
public:
    {$id}(const EventHandler &e);

    void init();
};


#endif //HEARTHSTONERESOLVER_{$id|upper}_H
